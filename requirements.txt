# Core ML/Data Handling (needed by the loaded pipeline)
pandas>=1.3.0
numpy>=1.20.0
scikit-learn==1.3.2  # Match version used for training/saving pipeline
xgboost==1.7.6       # Match version used for training/saving pipeline
joblib>=1.1.0

# Web Framework & Server
Flask>=2.0.0
waitress>=2.1.0      # Production WSGI server (simple, cross-platform)
# Or use gunicorn>=20.1.0 (if deploying on Linux)

# Add other libraries if your pipeline *specifically* requires them at runtime